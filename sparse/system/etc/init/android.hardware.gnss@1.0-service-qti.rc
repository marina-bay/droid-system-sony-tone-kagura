service gnss_service /vendor/bin/hw/android.hardware.gnss@1.0-service-qti
    class main
    user gps
    # TODO: Can't find this anywhere, so disable it for now
    disabled
    group system gps radio
